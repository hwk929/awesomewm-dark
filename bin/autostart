#!/bin/sh

run() {
    if ! pgrep -f "$1"; then
        "$@" &
    fi
}

# Applications
run "picom"
run "lxsession"
run "nm-applet"
run "flameshot"
run "copyq"

# Screen config
run "/home/hwk/.config/screenlayout/dual-tv.sh"
run "/home/hwk/.config/awesome/pkg/theme/wallpaper"
